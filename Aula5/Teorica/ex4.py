"""
--------Função lambda------
-Funções mais simples, sem nome, chamada de funções lambda
-Elas podem ser escritas em uma só linha de código e dentro
do programa principal
"""

#Exemplo
res = lambda x: x * x
print(res(3))

#Exemplo 2
soma = lambda x, y: x + y
print(soma(3, 5))